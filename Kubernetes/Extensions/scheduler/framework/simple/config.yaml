apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration
leaderElection:
  leaderElect: false
clientConnection:
  kubeconfig: /Users/guoqi/.kube/config
profiles:
  - schedulerName: default-scheduler
    plugins:
      score:
        enabled:
          - name: PodState